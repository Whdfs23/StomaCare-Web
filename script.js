// Logika Sederhana untuk Proyek StomaCare

document.addEventListener('DOMContentLoaded', function () {
    console.log("StomaCare Script Loaded!");

    // Contoh: Validasi form sederhana
    const forms = document.querySelectorAll('form');
    forms.forEach(form => {
        form.addEventListener('submit', function (e) {
            // Kita biarkan navigasi HTML bekerja dulu untuk demo
            // console.log("Form dikirim...");
        });
    });

    // Efek interaktif lainnya bisa ditambahkan di sini
});